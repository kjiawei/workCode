1,使用无名管道， 在两个子进程间通信
		子进程1负责从键盘读取字符串并往pipe写入， 
		子进程2负责读出并显示成“GOT:****”
		单次通信

2,
		利用管道文件实现数据传输， 
		  进程1：循环从键盘输入数据，并写入到管道文件中，直到输入exit，退出循环
		  进程2：循环从管道文件中读取数据，如果读到的数据是exit，则退出循环

3,	
	a:用pipe产生一个无名管道之后，再连续产生两个子进程A、B，
 	b:子进程A 在往管道写了一句hello 后就退出了
  	c: 子进程B读到A写入的数据，再往管道中写入hello world
  	d: 主进程读取最后管道中的数据

	进阶，利用fifo实现第3题


4，自己模拟系统日志，完成多个进程同时往一个日志文件（自定义，普通文件）写入数据

		